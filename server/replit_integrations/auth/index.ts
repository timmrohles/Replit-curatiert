export { setupAuth, isAuthenticated, getSession, providerConfig } from "./replitAuth";
export { authStorage, type IAuthStorage } from "./storage";
export { registerAuthRoutes, requireRole } from "./routes";
